---
import BaseLayout from '../../layouts/BaseLayout.astro';
import TextLogo from '../../components/ui/TextLogo.astro';
import { checkDashboardAccess } from '../../lib/dashboard-gate';

const { userId } = await Astro.locals.auth();
if (!userId) {
  return Astro.redirect('/sign-in');
}

// Gate: if user actually has an application or is accepted, redirect them appropriately
const gate = await checkDashboardAccess(userId);

if (gate.status === 'accepted') {
  return Astro.redirect('/dashboard/team');
}
if (gate.status === 'pending') {
  return Astro.redirect('/dashboard/pending');
}
---

<BaseLayout title="No Application Found | Hacklanta">
  <main class="gate-page relative flex min-h-screen flex-col items-center justify-center overflow-hidden bg-[#121212] px-4 py-12 sm:px-6 sm:py-20">

    <!-- Grid background -->
    <div class="pointer-events-none absolute inset-0 gate-grid opacity-[0.35]" aria-hidden="true"></div>

    <!-- Radial glow -->
    <div class="pointer-events-none absolute inset-0" aria-hidden="true">
      <div class="absolute left-1/2 top-1/2 h-[600px] w-[600px] -translate-x-1/2 -translate-y-1/2 rounded-full bg-[radial-gradient(circle,rgba(196,30,58,0.05)_0%,transparent_70%)] sm:h-[800px] sm:w-[800px]"></div>
    </div>

    <!-- Decorative compass / star — hidden on small screens -->
    <div class="pointer-events-none absolute left-[6%] bottom-[15%] hidden select-none opacity-[0.06] sm:block" aria-hidden="true">
      <svg width="140" height="140" viewBox="0 0 100 100" fill="none" stroke="currentColor" class="text-white">
        <circle cx="50" cy="50" r="35" stroke-width="0.5" />
        <circle cx="50" cy="50" r="25" stroke-width="0.3" />
        <line x1="50" y1="10" x2="50" y2="90" stroke-width="0.5" />
        <line x1="10" y1="50" x2="90" y2="50" stroke-width="0.5" />
        <line x1="22" y1="22" x2="78" y2="78" stroke-width="0.3" />
        <line x1="78" y1="22" x2="22" y2="78" stroke-width="0.3" />
        <path d="M50 15 L53 47 L85 50 L53 53 L50 85 L47 53 L15 50 L47 47 Z" stroke-width="0.5" fill="currentColor" opacity="0.3" />
      </svg>
    </div>

    <!-- Floating suit SVGs — hidden on small screens -->
    <div class="pointer-events-none absolute right-[8%] top-[15%] hidden select-none opacity-[0.03] sm:block" aria-hidden="true">
      <svg width="100" height="100" viewBox="0 0 100 100" fill="currentColor" class="text-red">
        <path d="M50 15 C25 30 10 55 30 70 C38 76 45 72 50 65 C55 72 62 76 70 70 C90 55 75 30 50 15 Z"/>
      </svg>
    </div>
    <div class="pointer-events-none absolute left-[10%] bottom-[25%] hidden select-none opacity-[0.02] sm:block" aria-hidden="true">
      <svg width="70" height="70" viewBox="0 0 100 100" fill="currentColor" class="text-white">
        <path d="M50 10 C30 10 15 25 15 40 C15 55 30 65 50 55 C70 65 85 55 85 40 C85 25 70 10 50 10 Z M35 60 C20 65 15 80 25 90 L50 75 Z M65 60 L50 75 L75 90 C85 80 80 65 65 60 Z"/>
      </svg>
    </div>

    <div class="relative z-10 mx-auto w-full max-w-4xl">

      <!-- Top nav -->
      <nav class="mb-10 flex items-center justify-between sm:mb-16">
        <a href="/" class="transition-opacity hover:opacity-70" aria-label="Home">
          <TextLogo size="xs" class="opacity-80" />
        </a>
        <a href="/" class="font-mono text-[10px] uppercase tracking-[0.25em] text-white/40 transition-colors hover:text-white/70">
          &larr; Home
        </a>
      </nav>

      <!-- Main typography block -->
      <div class="mb-10 sm:mb-16">
        <!-- "No application" — outline italic with hover fill -->
        <div class="gate-outline-wrap relative mb-1 cursor-default sm:mb-2">
          <!-- Glow layer -->
          <h1
            class="gate-outline-glow absolute inset-0 font-serif italic text-[clamp(2.6rem,12vw,7rem)] leading-[0.85] tracking-tight"
            aria-hidden="true"
          >No application</h1>
          <!-- Sharp layer -->
          <h1
            class="gate-outline relative font-serif italic text-[clamp(2.6rem,12vw,7rem)] leading-[0.85] tracking-tight"
          >No application</h1>
        </div>

        <!-- "was" — red italic accent with hover pop + tilt -->
        <div class="mb-0.5 ml-[2%] sm:mb-1 sm:ml-[5%]">
          <span class="gate-accent inline-block font-serif italic text-[clamp(1.8rem,5vw,3rem)] text-red-bright">was</span>
        </div>

        <!-- "found" — solid white serif with hover glow -->
        <div class="ml-[6%] sm:ml-[30%]">
          <h2 class="gate-solid cursor-default font-serif text-[clamp(3.2rem,16vw,9rem)] leading-[0.85] tracking-tight text-white">
            found
          </h2>
        </div>
      </div>

      <!-- Divider -->
      <div class="mb-8 h-px w-full bg-gradient-to-r from-transparent via-white/10 to-transparent sm:mb-12"></div>

      <!-- Content -->
      <div class="grid gap-8 sm:grid-cols-2 sm:gap-12">
        <div>
          <p class="mb-4 font-body text-sm leading-relaxed text-white/60 sm:mb-6 sm:text-[15px]">
            We couldn't find an application linked to your account. If you haven't applied yet, submit your application to get started.
          </p>
          <p class="font-body text-sm leading-relaxed text-white/50 sm:text-[15px]">
            If you already submitted a Tally form, make sure you used the same email address you signed up with.
          </p>
        </div>

        <div class="space-y-5 sm:space-y-6">
          <div class="border-l-2 border-red/30 pl-4 sm:pl-5">
            <h3 class="mb-1 font-mono text-[10px] uppercase tracking-[0.25em] text-red-bright">Apply now</h3>
            <p class="font-body text-xs text-white/50 sm:text-sm">
              Submit your application to unlock access to the team matcher and dashboard.
            </p>
          </div>
          <div class="border-l-2 border-white/10 pl-4 sm:pl-5">
            <h3 class="mb-1 font-mono text-[10px] uppercase tracking-[0.25em] text-white/60">Need help?</h3>
            <p class="font-body text-xs text-white/50 sm:text-sm">
              Reach out at <a href="mailto:official@progsu.com" class="text-red-bright/80 underline decoration-red/30 transition-colors hover:text-red-bright">official@progsu.com</a>
            </p>
          </div>
        </div>
      </div>

      <!-- Terminal accent -->
      <div class="mt-10 rounded border border-white/[0.06] bg-white/[0.02] px-4 py-3 font-mono text-[10px] leading-relaxed text-white/30 sm:mt-16 sm:px-5 sm:py-4 sm:text-[11px]">
        <span class="text-red-bright/60">$</span> access_status --user=current<br />
        <span class="text-white/40">&gt; STATUS: NO_APPLICATION</span><br />
        <span class="text-white/40">&gt; ACCESS_LEVEL: RESTRICTED</span><br />
        <span class="text-white/40">&gt; MESSAGE: Submit an application to continue...</span>
      </div>

      <!-- CTA -->
      <div class="mt-8 flex flex-wrap items-center gap-4 sm:mt-10 sm:gap-6">
        <a
          href="/apply"
          class="group inline-flex items-center gap-3 border border-red/40 bg-red/10 px-6 py-3 font-mono text-[10px] uppercase tracking-[0.2em] text-red-bright transition-all duration-300 hover:border-red/70 hover:bg-red/20 hover:shadow-[0_0_30px_rgba(196,30,58,0.15)] sm:px-8 sm:text-[11px]"
        >
          Apply now
          <svg class="h-3.5 w-3.5 transition-transform duration-300 group-hover:translate-x-1" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7" />
          </svg>
        </a>
        <a
          href="/"
          class="font-mono text-[10px] uppercase tracking-[0.2em] text-white/40 transition-colors hover:text-white/70 sm:text-[11px]"
        >
          &larr; Back to home
        </a>
      </div>

    </div>
  </main>

  <style>
    .gate-grid {
      background-image:
        linear-gradient(to right, rgba(255,255,255,0.03) 1px, transparent 1px),
        linear-gradient(to bottom, rgba(255,255,255,0.03) 1px, transparent 1px);
      background-size: 40px 40px;
    }

    @media (min-width: 640px) {
      .gate-grid {
        background-size: 60px 60px;
      }
    }

    /* Outline text — sharp stroke */
    .gate-outline {
      -webkit-text-stroke: 1px rgba(255,255,255,0.7);
      color: transparent;
      transition: color 0.4s ease, -webkit-text-stroke-color 0.4s ease, filter 0.4s ease;
    }

    @media (min-width: 640px) {
      .gate-outline {
        -webkit-text-stroke-width: 1.5px;
      }
    }

    /* Outline text — glow behind */
    .gate-outline-glow {
      -webkit-text-stroke: 5px rgba(255,255,255,0.06);
      color: transparent;
      transition: -webkit-text-stroke-color 0.4s ease;
    }

    @media (min-width: 640px) {
      .gate-outline-glow {
        -webkit-text-stroke-width: 8px;
      }
    }

    /* Hover: outline fills in + red glow */
    .gate-outline-wrap:hover .gate-outline {
      color: rgba(230, 57, 70, 0.08);
      -webkit-text-stroke-color: #E63946;
      filter: drop-shadow(0 0 25px rgba(230, 57, 70, 0.35));
    }

    .gate-outline-wrap:hover .gate-outline-glow {
      -webkit-text-stroke-color: rgba(230, 57, 70, 0.15);
    }

    /* Solid text — hover glow */
    .gate-solid {
      transition: filter 0.4s ease, text-shadow 0.4s ease;
    }

    .gate-solid:hover {
      filter: drop-shadow(0 0 20px rgba(230, 57, 70, 0.3));
      text-shadow: 0 0 40px rgba(230, 57, 70, 0.2);
    }

    /* Accent text — hover pop + tilt + glow */
    .gate-accent {
      cursor: default;
      transform-origin: center center;
      transition: transform 0.35s cubic-bezier(0.34, 1.56, 0.64, 1), filter 0.4s ease, text-shadow 0.4s ease;
    }

    .gate-accent:hover {
      transform: scale(1.2) rotate(-6deg);
      filter: drop-shadow(0 0 15px rgba(230, 57, 70, 0.5));
      text-shadow: 0 0 30px rgba(230, 57, 70, 0.3);
    }
  </style>
</BaseLayout>
