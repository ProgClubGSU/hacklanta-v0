---
import BaseLayout from './BaseLayout.astro';
import Nav from '../components/landing/Nav.astro';
import '../styles/global.css';

interface Props {
  title?: string;
}

const { title = 'Hacklanta — Go All In' } = Astro.props;
---

<BaseLayout title={title}>
  <Nav />
  <main>
    <slot />
  </main>
</BaseLayout>
